# Supported protocol and packet formats

 * `pcap` PCAP format, only the older 
 * `ip` IPv4 and IPv6, with fragmentation but excluding other options
 * `tcp` TCP
 * `udp` UDP
 * `dns` DNS over TCP and UDP
